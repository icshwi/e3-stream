# One should install libusb-1.0.0
# Debian apt-get install libusb-1.0-0-dev libusb-1.0-0
# USR_INCLUDES 
# $ pkg-config --cflags libusb-1.0
#   -I/usr/include/libusb-1.0
# USR_LDFLAGS
# $ pkg-config --libs libusb-1.0
# $ -lusb-1.0 
#
#ifeq (linux-x86_64, $(T_A))
#  DRV_USBTMC=YES
#  export DRV_USBTMC
#endif

# We always build PCRE based on stream repository no matter our HOST/TARGET
# However, leave that option, in order to update the future demands
# Tuesday, February 13 17:15:10 CET 2018, jhlee

# stream.Makefile
# RULES_PCRE
# RULES_E3

BUILD_PCRE=YES
